import { type CardContextType } from './interface';
/**
 * 🎯 SOLUCIÓN: Definición de la constante de Contexto.
 * Se exporta sola para evitar la advertencia de Fast Refresh.
 */
export declare const CardContext: import("react").Context<CardContextType | undefined>;
